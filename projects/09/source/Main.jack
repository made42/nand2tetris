/**
 * Main class of the Tetris game.
 */
class Main {
  
  /** Initializes a new game and starts it. */
  function void main() {
    var TetrisGame game;
    let game = TetrisGame.new();
    do game.run();
    do game.dispose();
    return;
  }
} 